{
  "name": "gutenku",
  "devDependencies": {
    "husky": "^8.0.3"
  },
  "scripts": {
    "pre-commit": "yarn --cwd packages/server lint && yarn --cwd packages/front lint",
    "prepare": "husky install"
  }
}