# GutenKu Api

## Installation

``` bash
cp .env.dist .env
yarn install
```

Batch:

``` bash
mkdir .cache
pip install python-dotenv
pip install pymongo
```

MongoDB:

``` bash
docker-compose up
```

## Usage

``` bash
cd bin
python3 fetch_books.py
```

## Run

``` bash
yarn dev
```